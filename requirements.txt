# FritzTrack4U dependencies
#
# The CORE daemon runs on the Python standard library alone (Python 3.9+) —
# no dependencies needed for SQLite history + console output.
#
# paho-mqtt is ONLY required if you enable MQTT publishing to Home Assistant
# (mqtt.enabled = true in config.json). It is imported lazily and the daemon
# degrades gracefully if it is missing.

paho-mqtt>=1.6
